# Archive

Ces guides et scripts SQL sont obsolètes et conservés uniquement pour l'historique du projet.
La référence à jour est `CLAUDE.md` à la racine du dépôt (schéma dans `SUPABASE_TABLE_SETUP.sql`, migrations dans `supabase/migrations/`).
